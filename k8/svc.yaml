apiVersion: v1
kind: Service 
metadata:
  name: myapploadbalancer
  labels:
    app: frontend-nginxapp
spec:
  type: LoadBalancer # ClusterIp, # NodePort
  selector:
    app: myapp
  ports: 
    - name: http
      port: 80 # Service Port
      targetPort: 80
